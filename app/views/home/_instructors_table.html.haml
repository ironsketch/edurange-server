%table.table.table-hover#instructors-table
  %thead
    %tr
      %th{ colspan: 3 }
        Instructors
        %span.small= link_to "create instructor +", "#modal-create-instructor", :data => {:toggle=>"modal"}, |
                onclick: "$('#modal-create-instructor').find('#email').val('');" +                  |
                         "$('#modal-create-instructor').find('#name').val('');" +                   |
                         "$('#modal-create-instructor').find('#organization').val('');"             |
    %tr
      %th Name
      %th Email
      %th Organization
      -#%th Options
  %tfoot
    %tr
      %th Name
      %th Email
      %th Organization
      -#%th Options
  - @instructors.each do |user|
    = render 'admin/instructor', user: user
